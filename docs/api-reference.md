# API Reference (placeholder)

Will document all `backend/app/api/v1/endpoints/` routes once implemented,
including:

- `POST /upload` — upload CSV datasets
- `POST /reconcile` — run the reconciliation pipeline
- `GET /metrics` — reconciliation KPIs
- `GET /exceptions` — unresolved exceptions with AI explanations
- `GET /reports/summary` — operational report
- `POST /chat` — natural-language query over uploaded data

Request/response schemas will be generated from `backend/app/schemas/` once
those are implemented.
