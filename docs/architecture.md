# Architecture Overview (placeholder)

This document will describe:

- The Clean Architecture layering used in `backend/app/`
- The reconciliation pipeline (ingest → validate → match → detect exceptions
  → compute metrics → AI-explain → report)
- The AI explanation pipeline and prompt design
- Sequence/flow diagrams for a single reconciliation run
- Data flow between frontend, backend, and datasets

To be filled in as implementation progresses.
